A Laravel 5 facade for the yotpo-php api interface
======

Usage:

Add the following to "config/services.php"
`'yotpo' => [
`        'app_key' => '<your app key here>',
`        'secret' => '<your secret key here>'
`    ],